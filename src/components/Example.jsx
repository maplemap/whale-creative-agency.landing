import React from 'react';

const Example = () => {
  const style = {
    height: '1000px',
    textAlign: 'center',
    borderTop: '1px solid'
  };

  return (
    <div style={style}>no sad</div>
  );
};

export default Example;
